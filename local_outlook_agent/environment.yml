name: outlook-agent
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - python-dotenv
      - openai
      - extract-msg
      - streamlit
      - 'pywin32; platform_system == "Windows"'
